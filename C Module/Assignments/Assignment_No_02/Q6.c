#include<stdio.h>
void main()
{
	char ch = 'p';
	if (ch >= 'A' && ch <= 'Z') 
	{
        printf("%c is Uppercase", ch);
    }  
    else
	{
       printf("%c is Lowercase", ch);
    }
    
        

}