void main()
{
	//8.input the length and width of a rectangle and find its perimeter.	
	
	int length = 10;   
	int width = 5;     
	int perimeter;

	perimeter = 2 * (length + width);
	printf("Perimeter of rectangle = %d\n", perimeter);
	
}