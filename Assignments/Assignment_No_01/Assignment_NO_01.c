#include<stdio.h>
void main()
{
	/*
	//1.add two integers and display the result
	int a = 10 ,b = 20;
	printf("Addition of two no is : %d",a+b);
	
	//2.C program to find the area of a circle.
	float radius = 5.0 ,PI = 3.14; 
	float area;
    area = PI * radius * radius;
    printf("Radius: %.2f\n", radius);
    printf("Area of the circle: %.2f\n", area);
    
	//3.to convert temperature from Celsius to Fahrenheit
	float C = 30.0;        
	float F;
	F = (C * 9.0 / 5.0) + 32;
	printf("Celsius: %.2f, Fahrenheit: %.2f\n", C, F);
	
	//4.swap two numbers using a temporary third variable.
	 int a = 10, b = 20, temp;
	    temp = a;
    	a = b;
    	b = temp;
    printf("After swapping: a = %d, b = %d\n", a, b);
    
    //5.Write a C program to input five numbers and find their average.
    int num1 = 10, num2 = 20, num3 = 30, num4 = 40, num5 = 50;
	float sum, average;
	
	sum = num1 + num2 + num3 + num4 + num5;
	average = sum / 5;
	printf("Average = %.2f", average);
	
	//6.find the square and cube of a given number.
	int num = 5;
	int square, cube;

	square = num * num;
	cube = num * num * num;

	printf("Square = %d\n", square);
	printf("Cube = %d\n", cube);
	
	//7.convert given minutes into hours and remaining minutes.
		
	int totalMinutes = 130;  
	int hours, minutes;

	hours = totalMinutes / 60;
	minutes = totalMinutes % 60;

	printf("Hours = %d, Minutes = %d\n", hours, minutes);
	
	//8.input the length and width of a rectangle and find its perimeter.	
	
	int length = 10;   
	int width = 5;     
	int perimeter;

	perimeter = 2 * (length + width);
	printf("Perimeter of rectangle = %d\n", perimeter);
	
	//9.input the base and height of a triangle and calculate its area.
	
	int base = 8;
	int height = 6;
	float area;

	area = 0.5 * base * height;
	printf("Area of triangle = %.2f\n", area);
	
	//10. Winput marks of five subjects, find the total marks, and calculate the percentage.
	
	int m1 = 75, m2 = 82, m3 = 90, m4 = 68, m5 = 85;
	int total;
	float percentage;

	total = m1 + m2 + m3 + m4 + m5;
	percentage = (total / 5.0);

	printf("Total Marks = %d\n", total);
	printf("Percentage = %.2f%%\n", percentage);*/


	
	


	
}